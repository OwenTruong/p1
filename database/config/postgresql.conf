# ----------------------------------------
# PostgreSQL Core Custom Configuration File
# ----------------------------------------

# Connections and Authentication
listen_addresses = '*'          # Listen across all available network interfaces
max_connections = 100            # Standard concurrent connection pool limit

# Memory and Resource Allocation
shared_buffers = 128MB          # Tune engine caching based on alpine base constraints
dynamic_shared_memory_type = posix